🚀 คู่มือการติดตั้งและรันโปรเจกต์ (Setup Guide)
โปรเจกต์นี้ใช้ Robot Framework ร่วมกับ Selenium ในการทดสอบ To-Do List โดยรันผ่าน Chrome (Headless) บน Windows ครับ
1. การเตรียมเครื่อง (Prerequisites)
  ติดตั้ง Python 3.10 หรือเวอร์ชันใหม่กว่า
  ติดตั้ง Google Chrome Browser (ตำแหน่งมาตรฐาน: C:\Program Files\Google\Chrome\Application\chrome.exe)
2. ขั้นตอนการติดตั้ง (Installation)
  เปิด Terminal หรือ PowerShell ในโฟลเดอร์โปรเจกต์แล้วทำตามนี้ครับ:
   2.1 สร้าง Virtual Environment (venv): เพื่อแยก Library ของโปรเจกต์ออกจากเครื่องหลัก
      python -m venv venv
   2.2 เปิดใช้งาน venv (Activate):
     Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope Process
     .\venv\Scripts\Activate.ps1
   2.3 ติดตั้ง Library ที่จำเป็น
     pip install -r requirements.txt
3. วิธีการรันการทดสอบ (Running Tests)
   เมื่อติดตั้งเสร็จแล้ว ให้ใช้คำสั่งรันด้านล่างนี้:
     robot -d results tests/todo_test.robot
4. การตรวจสอบผลการทดสอบ (View Reports)
หลังจากรันเสร็จ คุณสามารถเปิดดูผลลัพธ์ผ่าน Browser ได้จากไฟล์ในโฟลเดอร์ results:
  - report.html: สรุปภาพรวม Pass/Fail และสถิติการรัน
  - log.html: แสดงรายละเอียดขั้นตอนการคลิกและรูปภาพหน้าจอ (Screenshot) หากเกิดข้อผิดพลาด
