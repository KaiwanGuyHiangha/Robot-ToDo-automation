📝 Robot To-Do List Automation
โปรเจกต์ Automated Testing สำหรับทดสอบเว็บไซต์ To-Do List พัฒนาด้วย Robot Framework (Selenium)
🚀 วิธีการติดตั้งและรันการทดสอบ (PowerShell)
ก๊อปปี้ข้อความด้านล่างนี้ไปวางใน Terminal ของโปรเจกต์เพื่อเตรียมระบบและเริ่มรันเทสทันที:
powershell
# 1. สร้างและเข้าสู่ Virtual Environment -> 2. ติดตั้ง Library -> 3. เริ่มรันเทส
python -m venv venv; Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope Process; .\venv\Scripts\Activate.ps1; pip install -r requirements.txt; robot -d results tests/todo_test.robot
ใช้โค้ดอย่างระมัดระวัง

📋 ขั้นตอนแบบละเอียด (Step-by-Step)
1. เตรียมสภาพแวดล้อม:
powershell
python -m venv venv
.\venv\Scripts\Activate.ps1
ใช้โค้ดอย่างระมัดระวัง

2. ติดตั้ง Library:
powershell
pip install -r requirements.txt
ใช้โค้ดอย่างระมัดระวัง

3. รันการทดสอบ:
powershell
robot -d results tests/todo_test.robot
ใช้โค้ดอย่างระมัดระวัง

📊 การดูผลการทดสอบ
เมื่อรันเสร็จสิ้น สามารถดูรายงานสรุปผลได้ที่:
results/report.html (สรุปผ่าน/ไม่ผ่าน)
results/log.html (รายละเอียดการทำงานอย่างละเอียด)
⚙️ ข้อมูลเพิ่มเติม
Browser: รองรับการรันแบบ Headless Mode (รันเบื้องหลัง) บน Google Chrome
CI/CD: มีการตั้งค่า GitHub Actions ให้รันเทสอัตโนมัติทุกครั้งที่ Push โค้ด
